<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.llw.demo_neihangdzi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SercherActivity" />
        <activity android:name="myactivity.com.OneActivity" />
        <activity android:name=".M1Activity" />
        <activity android:name="myactivity.com.TwoActivity" />
        <activity android:name="myactivity.com.ThreeActivity"></activity>
    </application>

</manifest>